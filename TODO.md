# TODO

## In Progress
- [ ] Get a labelled acoustic dataset + trained tile-grade model out of the **Acoustic-Analysis** app and into `acoustic_node`. The app itself (submodule at `Acoustic-Analysis/`, independent repo <https://github.com/KL-Mithunvel/Acoustic-Analysis>, added 2026-09-08) is **v0.1-built** as of 2026-09-09 — DSP suite, feature extraction, rule-based grader, SQLite dataset store, headless CLI, 12-screen Tkinter GUI, 133 tests (its own `TODO.md` / `.CLAUDE/CLAUDE-LOG.md` track app-internal work). What remains, all gated on real tiles + a pistonphone being on the bench: (1) run it against a real measurement mic, (2) pistonphone calibration so levels are dB SPL, (3) record good + cracked + corner-broken tiles from SMTW, (4) build a reference profile + validate grading, (5) export the labelled dataset and train a classifier, (6) copy **only the trained model** (never the app code — one direction, copy-as-needed) into `acoustic_node`.
- [ ] Decide `acoustic_node`'s runtime analysis path. The full DSP + quality-classification pipeline from `documents/project/Acoustic_Analysis_Methods.md` (Part D modules: `calibration.py`, `octave_bands.py`, `decay.py`, `weighting.py`, `sound_level.py`, `features.py`, `reference_profile.py`, `classifier.py`) is **built and tested in the `Acoustic-Analysis` submodule**, not yet in `acoustic_node` (which still has only `signal_processing.py`). Open question: does the node run a copied trained model only (methods doc's stated intent), or does it also need a hand-ported subset of the analysis code to compute the feature vector that model consumes at inference time? Resolve once a model exists. The one-time NC survey of the enclosure (methods doc Part E step 6) still belongs here regardless. Gated on real tiles + a calibrator being on the bench.
- [ ] Software-level novelty exploration (AI anomaly diagnosis + adaptive grading) — thinking-stage notes only, no code (`documents/project/AI_Software_Novelty.md`, 2026-08-20), prompted by Review 1 (2026-08-19) feedback that the project needs more novelty. Idea A (LLM-based fault/anomaly summarization) depends on the database logging layer below landing first; Idea B (adaptive/no-pretraining grading model) is a longer-running research thread gated on real labeled defect data existing.
- [ ] Pick-and-place HMI / digital twin dashboard — standalone prototype **built** (`pick_place_node/python/pick_place/`: `MockGantrySimulator` + Three.js browser twin `twin_prototype.py` + matplotlib desktop twin `desktop_twin.py`; `MockGantrySimulator` unit-tested). Feature sketch in `documents/project/Pick_Place_Sorting_Subsystem.md`. Remaining: swap `MockGantrySimulator` for a real `GantryWorker` reading live pose over the ASCII protocol, add the control panel (jog/home/teach/e-stop) to the page, wire it to the master — all gated on the firmware + `pick_place/` control modules below existing.
- [ ] Review 0 (VIT Zeroth Review Form, hardcopy) — content drafted (`documents/deliverables/review_0/Review_0_Form.md`), still needs guide + HoD signature on the physical hardcopy, submission on/before 2026-07-13, then update guide + project title in VTOP. See `documents/deliverables/review_0/README.md`.
- [ ] Review 1 (Expert Panel Review, 2026-08-19 tentative) — narrative content drafted per rubric category (`documents/deliverables/review_1/Review_1_Content.md`, 2026-08-03). Still missing: literature survey (none written), sustainability goals treatment, and Phase 1 results/analysis (lab work not done yet) — see that file's open questions. Needs guide approval well in advance per college guidelines.

## Done
- [x] Measured the trained tile-grade models' real edge cost, staged one into `camera_node`, and wired it in as an optional second opinion (2026-09-20). Added `camera_models/cam_edge/` — trains nothing, only measures: `benchmark.py` (speed/memory across thread counts, incl. `camera_node`'s existing per-frame/per-tile CV cost for scale), `evaluate.py` (accuracy on one common 76-image val split with Wilson 95% CIs + per-class recall), `stage_model.py` (promotes one `.onnx` + a metadata sidecar into the git-tracked `camera_node/models/`). Depends only on onnxruntime/opencv/numpy/Pillow/psutil so the identical script runs on the Pi. Headline: `yolo26n-cls` 82.9% @ 1.59 ms / 6.2 MB beat `yolo26s-cls` (85.5% @ 4.16 ms, 7x larger — the gap sits inside both CIs) and `vit-base` (92.1% but 110 ms, 343 MB); **dynamic INT8 made the YOLO nets both ~12x slower and far worse** (82.9% → 57.9%; `yolo26s` INT8 stopped predicting class 3B entirely). Staged `yolo26n-cls`. Then wired it into the node: new `camera/grading_model.py` (lazy `onnxruntime` import, `from_config()` returns None for every not-set-up case), `config.yaml`'s `grading_model` block (**`enabled: false` by default**), `model_grade`/`model_confidence`/`model_probabilities`/`model_inference_ms` on `TileRecord` recorded *alongside* — never replacing — the rule-based grade, plus dashboard and `process_video.py --report` columns. Verified end-to-end through `camera_node`'s own path: 82.89% reproduced, 100% label agreement with `cam_edge`'s runtime, 0.00000000 preprocessing tensor diff. 36 new unit tests that deliberately **don't** require onnxruntime. Pi deploy gained `install_pi.sh --with-grading` + `python3-pil` (both unrun).
- [x] Replaced "grade the tile once it has left the frame" with a configurable capture trigger (2026-09-20). New pure `camera/line_trigger.py` (`LineCrossingDetector`) fires on the frame a tile's bounding-box centre crosses a configured line, in normalized 0.0-1.0 coordinates so one config value holds at any resolution, with a hysteresis deadband against segmentation jitter and a `direction` filter so a tile nudged backwards isn't graded twice. `config.yaml`'s `processing_trigger` selects it (`"line_crossing"`, now the default) or the old behaviour (`"departure"`, kept for footage from an unfixed camera mount); `worker.py` and `process_video.py` both honour it, so offline results match the live station. `TileRegion` gained `center`/`centroid`/`center_normalized` (bbox centre, not area centroid — a broken corner would shift the centroid and trip the line at a different moment than an intact tile). Dashboard draws the line + the tile's centre crosshair and flashes on the firing frame. Smoke-tested on a synthetic clip: capture centres x=340 for all four tiles under `"line_crossing"` vs. 120/520/520/180 under `"departure"` — the inconsistency the trigger exists to remove. Unit tests in `tests/test_camera_line_trigger.py`. **Never run against real conveyor footage or a live camera** — see Not Started.
- [x] Decided the camera-vs-acoustic Raspberry Pi swap and restructured `camera_node/` for it (2026-09-20). **Camera moves to a Raspberry Pi 5 (16 GB), Raspberry Pi OS Desktop 64-bit; acoustic stays on the UNO Q.** Reasoning recorded in `Automation_Architecture.md` §5.2 (full comparison table) / §5.3 / §5.7 (App Bricks convention amended to "UNO Q nodes only"): only acoustic uses the UNO Q's real-time MCU half (ToF → ARM/LOCK solenoid ball-drop → capture window), only camera is compute-bound (per-frame segmentation + a per-tile CNN/ViT classifier), and the 16 GB + PCIe are wasted on acoustic. Code changes: deleted `camera_node/app.yaml` + `sketch/`; `python/main.py` rewritten from an App Lab stub into a real systemd entry point (thin shim over `live_dashboard.main()`); `capture.py` gained `PiCameraCapture` (CSI/Picamera2, lazy import), `create_capture()` backend factory and `list_cameras()`; `config.yaml` gained `camera.backend`, `api_preference`, and a `picamera2` block (format/swap_rb/controls); `live_dashboard.py` gained `--list-devices` / `--backend` / `--no-dashboard`; `CameraWorker.join()` added for the headless path; new `camera_node/deploy/` (systemd unit template, `install_pi.sh`, `requirements-pi.txt`, README); new `.gitattributes` pinning `*.sh`/`*.service` to LF (repo is `core.autocrlf=true`, so the Pi would otherwise clone broken scripts). 57 tests still green; USB path + `main.py` + CLI smoke-tested on the dev laptop. **The Pi itself has not been set up and no Pi-specific code has ever run** — see Not Started.
- [x] Pick-and-place: decided the control architecture + built the digital-twin prototype + first UNO Q bring-up (2026-08-17 → 09-01). Architecture (`documents/programming/pick_place_control_protocol.md`, `documents/electrical/schematics/pick_place_hardware_connections_plan.md`, granular checklist `documents/pick_place_todo.md`): custom minimal UNO Q MCU firmware (not GRBL/Klipper — FR-18), Linux owns the coordinate model + sequencer + HMI, line-based ASCII protocol between them; motor/driver/PSU/gripper hardware all chosen (CNC Shield V3.10, 4× TMC2208, 4× 42HM48-1684, LRS-150-12, vacuum cup). Built: `pick_place_node/python/pick_place/` digital-twin HMI prototype (`MockGantrySimulator` pure sim + `TwinWorker` + Flask/Three.js browser twin + matplotlib desktop twin; `MockGantrySimulator` unit-tested, 57 total). Confirmed on the lab UNO Q (2026-09-01): MCU is Zephyr/STM32U585, `arduino-app-cli` build/flash/run works (~90 s), `Arduino_RouterBridge.h` ↔ Python `Bridge.call()` bridge works, a teammate's motor sketch runs as app `user:cnc`. Added `bench_tests/stepper_smoke_test/` (4-motor bench app) + `tools/uno_q/push-stepper-test.bat`. No firmware, no `pick_place/` control modules, nothing on the physical rig yet — see Not Started.
- [x] Decided + built the acoustic station's ToF-triggered dual-solenoid (ARM+LOCK) gravity-drop tap mechanism: full pin map/BOM/wiring diagram (`documents/electrical/schematics/acoustic_station_wiring.md`), MCU-side state machine (`acoustic_node/sketch/sketch.ino`), pure Python FSM mirror + dev-machine tap simulator (`acoustic_node/python/acoustic/tap_sequencer.py`), external-tap-event audio capture (`acoustic_node/python/acoustic/hardware_trigger.py`), `AudioCapture`/`config.yaml` trigger-mode switch (`rms`/`hardware`/`simulated`), `live_monitor.py --simulate-taps` driver, and reconciled `project_charter.md` §6.2 / `Acoustic_Sorting_Subsystem.md` §3.1 / `requirements.md` FR-21 with the decision. 11 new synthetic-input unit tests, all passing (57 total). Physical hardware (real ToF sensor, solenoids, App Bricks bridge) still not on the bench — see Not Started (2026-08-20)
- [x] Set up venv + requirements.txt (sounddevice, numpy, scipy, matplotlib, pyyaml, pytest)
- [x] Build acoustic capture module: RMS-trigger detection, pre-trigger buffer, FFT + waveform plotting, CLI (`acoustic/`)
- [x] Unit tests for signal processing and trigger logic (`tests/`) — 11 passing
- [x] Fill in root `CLAUDE.md` (`.CLAUDE/CLAUDE.md`) for the acoustic module's current state
- [x] Restructure docs: moved `project/` → `documents/project/`, added `documents/{mech,electrical,programming,requirements}/` and `documents/deliverables/` (college coursework), drafted `documents/requirements/requirements.md`, updated `.CLAUDE/CLAUDE.md` accordingly (2026-07-10)
- [x] Set up SSH/VNC dev tooling for the lab Arduino UNO Q board (`tools/uno_q/`) — push code, terminal, and a fixed-resolution VNC desktop over an SSH tunnel; fixed a stale-session bug that was making TigerVNC ignore `-geometry` (2026-07-28)
- [x] Adopted the Arduino App Bricks folder convention (`app.yaml` + `sketch/` + `python/`) for every UNO Q station; migrated the working `acoustic/` package into `acoustic_node/python/acoustic/` unchanged (verified via `pytest.ini`'s `pythonpath` — all 11 tests still pass, CLI re-verified with `--list-devices`); scaffolded `camera_node/` and `pick_place_node/` as stubs; decided pick-and-place controller hardware = Arduino UNO Q (was undecided); updated `tools/uno_q/push.bat`/README, `Automation_Architecture.md` §5.6/§5.7, `requirements.md` FR-10a/FR-22 accordingly (2026-08-03)
- [x] Built the camera vision pipeline (`camera_node/python/camera/`): HSV-based tile segmentation, grayscale+Canny crack detection with length/severity measurement, corner fill-ratio broken-corner detection with missing-area measurement, a debounced tile-crossing counter, and a Flask dashboard streaming live video (MJPEG) + tile count + crack/corner results over WiFi (`host: 0.0.0.0`). 15 new synthetic-image/sequence unit tests, all passing (26 total in the suite); end-to-end pipeline + Flask routes smoke-tested with a synthetic image. Added `opencv-python-headless`/`Flask` to `requirements.txt`, added `*.jpg`/`*.jpeg`/`*.png` to `.gitignore`. All thresholds explicitly flagged provisional — no real tile photos were available on this machine to tune against (2026-08-07)

## Not Started
- [ ] Get the actual ToF sensor + 2x solenoids + MOSFET modules on the bench, wire per `documents/electrical/schematics/acoustic_station_wiring.md`, install a real ToF Arduino library and replace `sketch.ino`'s `readTofDistanceMm()` placeholder, and confirm the App Bricks bridge-variable read (`tapCount`) actually works from `python/main.py` — everything upstream of the physical hardware (sketch FSM, `tap_sequencer.py`, `hardware_trigger.py`, `config.yaml` trigger modes) is written and tested against synthetic input/simulated taps, but nothing has touched real hardware yet
- [ ] Decide ball material/mass and drop height (fixes impact energy `E = mgh`) and design the reload/reset path back to the ARM lift platform — both still open per `acoustic_station_wiring.md` §7
- [ ] Design the actual ARM lift + LOCK latch mechanical hardware (CAD) — the wiring doc's mechanism diagram is a conceptual placeholder, not engineered
- [ ] Run `python -m acoustic.live_monitor --calibrate` against the real built-in mic and set a real `trigger.rms_threshold` in `acoustic/config.yaml` (currently a placeholder value)
- [ ] Confirm Windows Microphone Enhancements (AGC/noise suppression) are disabled before trusting any calibration numbers
- [ ] Full end-to-end smoke test: `python -m acoustic.live_monitor` triggering on a real tap/clap and showing the plot
- [ ] Add WAV + spectrum saving to disk once ready to start building a labeled dataset (deferred by explicit decision — live-display-only for now)
- [ ] Get the camera vision thresholds (`camera_node/python/camera/config.yaml`) tuned against real tile photos — the user has sample crack/corner-break/good photos from a different camera than the one that'll actually be used, but they weren't accessible on this machine when the pipeline was built (2026-08-07); every HSV/Canny/length/fill-ratio value in there is currently a placeholder guess
- [ ] Run `python -m camera.live_dashboard` against a real webcam end-to-end (open the dashboard in a browser, confirm the MJPEG stream + tile count + crack table actually update) — only smoke-tested with a synthetic image and Flask's test client so far, no camera hardware available to the assistant
- [ ] **Set up the Raspberry Pi 5 camera station** (decided 2026-09-20, nothing done): image Raspberry Pi OS Desktop 64-bit, clone the repo, run `camera_node/deploy/install_pi.sh` (apt deps + `tile-camera` systemd unit — never executed, expect to fix package names), and confirm the service starts and the dashboard is reachable at `http://<pi>:5000/`. Fit an active cooler and watch `vcgencmd get_throttled` under continuous CV load.
- [ ] Decide the camera station's camera: CSI Raspberry Pi module (preferred — controllable optics/exposure, `camera.backend: "picamera2"`) vs. USB webcam (`"usb"`, `api_preference: "v4l2"`). Then re-check `camera.device_index` / `picamera2.camera_num` with `python -m camera.live_dashboard --list-devices` on the Pi.
- [ ] **Validate `PiCameraCapture` against a real CSI module** — written 2026-09-20 with no hardware present, never run. Specifically confirm the channel-order assumption (Picamera2's `"RGB888"` should already hand back B,G,R; if colours invert, flip `camera.picamera2.swap_rb`) — getting this wrong silently wrecks the calibrated HSV segmentation range instead of raising.
- [ ] Lock exposure / gain / white balance via `camera.picamera2.controls` once the enclosure lighting is final. With auto-exposure and AWB on, the sensor re-exposes per tile and the calibrated `hsv_lower`/`hsv_upper` stop meaning anything — a failure mode a laptop webcam pointed at a desk never showed. Worked example is in `config.yaml`.
- [ ] Measure the camera pipeline's real frame rate on Pi 5 hardware. `target_fps: 15` with per-frame segmentation + JPEG re-encode has never been load-tested on this CPU, and the Desktop image's optional kiosk browser is a second client on the same MJPEG stream.
- [ ] **Tune `trigger_line` on the real rig and validate the line trigger on real conveyor footage.** `position` depends on where the camera ends up mounted and `direction` on which way the belt runs, so neither can be set before the rig exists; `hysteresis: 0.02` was reasoned about (~13 px at 640 px wide), never measured against real segmentation jitter. Also unexercised: two tiles in frame at once — `segment_tile()` returns only the largest region, so the trigger currently assumes one tile at a time. Real phone footage of the belt already exists in `data/` (`development/process_conveyor_video.py`) and is the obvious first test.
- [ ] **Validate the ONNX grade model on tiles that are not intact, and on the station's own camera.** It is trained on 376 photos of **intact** tiles from a different camera at a different working distance, measured on 76 val images (82.9%, 95% CI 72.9-89.7 — about one tile in six misgraded; class "4" recall only 0.667). It classifies cosmetic grade tier and cannot see cracks or broken corners at all. Keep `grading_model.enabled: false` until it has been re-measured on real station imagery, and expect to retrain once the Pi's camera and enclosure lighting are fixed.
- [ ] Decide whether the ONNX grade model's answer should ever *influence* the station's grade, and how. Right now `model_grade` is recorded strictly alongside the rule-based `grade` with no fusion, deliberately, so the two can be compared on real tiles first. Fusing them is a decision for the master's grading layer (charter §10.1), not the camera node, and needs real labelled data before it is worth designing.
- [ ] Run `install_pi.sh --with-grading` on a real Pi and confirm `onnxruntime` actually installs. It is the one dependency apt doesn't package, so the script pip-installs it into the system interpreter with `--break-system-packages`; the wheel may not exist for the Pi's Python/arch, in which case try piwheels or a venv. Non-fatal by design — grading stays off and the station still inspects tiles.
- [ ] Decide whether/how to add crack-type taxonomy (hairline vs. structural, edge vs. center) and which-corner localization to the camera pipeline — deliberately deferred (`camera_node/README.md` Known Limitations) until real labeled photos are available to design against
- [ ] Decide whether the camera station also serves as the encoder-zero entry sensor, or a separate dedicated entry sensor still exists ahead of it (open question, charter §6.1)
- [ ] **Build** the gantry's custom machine-control layer — architecture is fully designed (`documents/programming/pick_place_control_protocol.md`: custom UNO Q MCU firmware, not GRBL/Klipper per FR-18; Linux-side coordinate model + sequencer; line-based ASCII protocol between them). Not written yet: the firmware (`pick_place_node/sketch/sketch.ino` is empty — FSM, step gen + accel, homing, limits, e-stop, ASCII parser) and the `pick_place/` modules (`protocol.py`, `coordinate_model.py`, `sequencer.py`, `gantry_backend.py`, `job_runner.py`). Granular phase-by-phase checklist: `documents/pick_place_todo.md` (nothing checked off — Phase 0 all needs the physical rig). Open risk: whether an accelerated stepper library builds under Zephyr on the UNO Q.
- [ ] Pick-and-place mechanical design + travel limits — **motor/driver/PSU/gripper hardware all chosen and in hand** (CNC Shield V3.10, 4× TMC2208, 4× 42HM48-1684 NEMA 17, Mean Well LRS-150-12, vacuum-cup + confirm sensor; 3 logical axes, Y beam = 2 motors with A cloned from Y in hardware — `documents/electrical/schematics/pick_place_hardware_connections_plan.md`). Still open: the gantry frame CAD, actual travel envelope, and stack pitch (blocked on SMTW tile size).
- [ ] Reconcile charter sections that still describe a solenoid directly striking the tile (§17 Safety, §19 Prototype Build Plan, §20 BOM, §25 Conclusion) with the ball-drop decision, now that the release mechanism itself is fixed
- [ ] Wire the **UNO Q** nodes' `python/main.py` (`acoustic_node/`, `pick_place_node/`) to their real modules and verify the App Lab `App.run()` pattern. (`camera_node/python/main.py` is done — it's a real systemd entry point on the Pi as of 2026-09-20, no App Lab involved.) The underlying `arduino-app-cli` build/flash/run flow and the `Arduino_RouterBridge.h` ↔ Python `Bridge.call()` bridge are **confirmed on the lab board (2026-09-01)** — but that used bare `arduino-app-cli` apps, not the `python/main.py` + `app.yaml` App-Lab shape, and both remaining `python/main.py` files are still unwired stubs (requirements.md FR-22). Acoustic's `main.py` also needs its `read_tap_count()` bridge read implemented against the confirmed API.
- [ ] Design the acoustic and dimensional stations' local monitor displays, plus the master overall dashboard (charter §14 Monitoring Architecture, requirements.md FR-19) — the camera station's is built (`camera_node/python/camera/dashboard.py`, requirements.md FR-25, 2026-08-07) and the pick-place station has a digital-twin prototype (`pick_place_node/python/pick_place/`, mock data); structure logged data (grade counts, defect breakdown) for real industrial reuse, not just prototype debugging (FR-20)
- [ ] Dimensional inspection module (charter §6.3) — not started; also needs a decision on camera-only measurement vs dedicated ToF/laser hardware (see `documents/requirements/requirements.md` FR-06)
- [ ] Conveyor + sorting execution (charter §7.1, §8) — not started. Sorting/handling is decided as a Cartesian gantry pick-and-place (charter §7.4) whose control layer is now designed (see the "Build the gantry's custom machine-control layer" item above); the conveyor itself (Arduino Mega) and how it hands tiles to the gantry are untouched.
- [ ] Database logging + dashboard (charter §9.4, §14) — not started
- [ ] Define/implement encoder-based tile tracking (`documents/project/Automation_Architecture.md` §8) — architecturally decided, not yet built or tested against a live conveyor
- [ ] Write `documents/project/hardware.md` — consolidated hardware architecture doc (sensors, actuators, wiring, BOM) across all subsystems. `documents/project/Acoustic_Sorting_Subsystem.md` and `documents/project/Camera_Sorting_Subsystem.md` already cover per-subsystem hardware in detail — check whether this should be a cross-subsystem summary/index over those rather than a new write-up from scratch
- [ ] Write `documents/project/software.md` — software architecture doc (module boundaries, interfaces, threading model) once more than the acoustic module exists
- [ ] Write `documents/project/communication.md` — inter-module/inter-device communication doc (Pi↔master PC, MQTT/Modbus/OPC UA choices). `documents/project/Automation_Architecture.md` §2 already sketches the master/node network topology (MQTT, per-station nodes) — this doc should likely expand on that rather than duplicate it
- [ ] Write `documents/project/control-flow.md` — machine operation sequence / state machine doc (charter §16, and the per-station sequences already detailed in `documents/project/Acoustic_Sorting_Subsystem.md` §2.1) once the conveyor/control layer is designed
- [ ] Populate `documents/mech/`, `documents/electrical/`, `documents/programming/` — currently scaffolding only (README + `.gitkeep`), no real CAD/electrical/flowchart content yet
- [ ] Get tile size/weight range from SMTW's actual product line — blocks conveyor width, gantry travel, gripper sizing, dimensional tolerances, and ball-drop energy calibration (see `documents/requirements/requirements.md` Open Items)
- [ ] Turn charter §20's generic example BOM into a real costed procurement list now that camera/acoustic/pick-place = UNO Q, conveyor = Mega, master = user's PC are confirmed, and the pick-place motor/driver/PSU BOM is known (`documents/project/project_charter.md` §20, `documents/electrical/schematics/pick_place_hardware_connections_plan.md`)
- [ ] Add a sustainability-goals treatment somewhere in the project docs — Review 1 rubric (`documents/deliverables/review_1/README.md`) requires it and nothing currently addresses it
- [ ] Get the guide's actual signature on the daily logbook entries — digital draft back-filled through 2026-08-03 (`documents/deliverables/logbook/Logbook.md`), still needs the physical/signed copy per the college guidelines
