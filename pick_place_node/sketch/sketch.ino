// Pick-and-place node MCU sketch — STUB, not implemented.
//
// Intended responsibility (project_charter.md §7.4 Decision, requirements.md FR-18):
// receive high-level semantic place commands from the master (grade/slot, not raw
// coordinates) and translate them into axis motion + gripper sequencing for the
// Cartesian gantry — explicitly NOT an off-the-shelf CNC/G-code controller
// (GRBL/Mach3/LinuxCNC). Axis count, motor type, gripper type, and travel limits are
// all undecided (TODO.md) — do not hardcode pins/kinematics here yet.

void setup() {
}

void loop() {
}
